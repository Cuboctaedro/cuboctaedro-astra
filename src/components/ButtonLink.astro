---
export interface Props {
    href: string
    label: string
}

const { href, label } = Astro.props;

---
<a href={href} target="_blank" class="px-8 py-4 inline-flex items-center justify-center bg-neutral-800 text-white hover:bg-red-700 transition-colors uppercase tracking-wide text-sm">{label}</a>
